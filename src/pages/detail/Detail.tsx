import styled from "styled-components"
import { useParams } from "react-router-dom"
import { useGetTrendingQuery } from "../../services/Services"

const Detail = () => {
  return <div>Detail</div>
}

export default Detail
